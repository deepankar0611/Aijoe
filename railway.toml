[build]
builder = "nixpacks"
buildCommand = "npm install --legacy-peer-deps && npm run build"

[deploy]
startCommand = "npm run start"
healthcheckPath = "/"
healthcheckTimeout = 100

[env]
NODE_ENV = "production"
NIXPACKS_NODE_VERSION = "20"
NIXPACKS_NPM_VERSION = "10.2.4" 